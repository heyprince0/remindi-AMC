The quotation view page at /quotations/:id is incomplete and broken.
Please rebuild this page completely from scratch. Keep all other 
pages untouched — only rebuild /quotations/:id.

Here is exactly what this page must have:

---

1. HEADER SECTION (top of page)

- Back arrow button (goes to /quotations list)
- Quote number displayed large and bold 
  Use: quotation.quote_no ?? ('QT-' + quotation.id)
- Date below quote number: formatted as "05 May 2026"
- Status badge top right: 
  Draft=gray, Sent=blue, Accepted=green, Rejected=red
- Row of action buttons:
  * Download PDF (blue button with download icon)
  * Share WhatsApp (green button with whatsapp icon)
  * Edit (outline button)
  * Change Status dropdown (Draft / Sent / Accepted / Rejected)
  * If status is Accepted: show "Convert to Invoice" button (purple)

---

2. COMPANY INFO SECTION

Fetch company profile from company_profile table where 
user_id = auth.uid()

Show:
- Company Name (bold)
- Tagline
- Address, Email, Phone
- GSTIN, PAN
- Logo if logo_url exists

---

3. CUSTOMER INFORMATION SECTION

Show:
- Client Name: quotation.client_name ?? '-'
- Client Address: quotation.client_address ?? '-'
- Client City: quotation.client_city ?? '-'
- Client GSTIN: quotation.client_gstin ?? '-'
- Subject: quotation.subject ?? '-'
- Valid Till: formatted date or '-'

---

4. ITEMS TABLE

Columns: SR. | Description | Qty | Unit Price | Amount

Data from: quotation.items (jsonb array)
Each item has: description (or particulars or name), qty, rate, amount

Use this safe mapping:
(quotation.items ?? []).map((item, index) => ({
  sr: index + 1,
  description: item.particulars ?? item.description ?? item.name ?? '-',
  qty: item.qty ?? 1,
  rate: Number(item.rate ?? 0),
  amount: Number(item.amount ?? (item.qty * item.rate) ?? 0)
}))

---

5. TOTALS SECTION

Subtotal: Number(quotation.subtotal ?? 0)
GST (18%): Number(quotation.sgst ?? 0) + Number(quotation.cgst ?? 0)

Grand Total calculation (IMPORTANT - fix the ₹0 bug):
const subtotal = Number(quotation.subtotal ?? 0)
const sgst = Number(quotation.sgst ?? 0)  
const cgst = Number(quotation.cgst ?? 0)
const grandTotal = (subtotal + sgst + cgst) > 0 
  ? (subtotal + sgst + cgst) 
  : subtotal

Show Grand Total in blue, large, bold.

All amounts formatted with: 
Number(value).toLocaleString('en-IN')

---

6. NOTES SECTION

Show additional notes if quotation.notes exists.

---

7. DOWNLOAD PDF FUNCTION

Use jsPDF + jspdf-autotable.
If not installed: npm install jspdf jspdf-autotable

Wrap entire PDF function in try/catch.

Safe value helpers — use these everywhere:
const safeStr = (val) => String(val ?? '-')
const safeNum = (val) => Number(val ?? 0).toLocaleString('en-IN')
const safeDate = (val) => val 
  ? new Date(val).toLocaleDateString('en-IN') 
  : '-'

PDF layout:
- Header: logo (if exists) + company name + address + phone
- Line separator in theme color 
  (profile.theme_color ?? '#185FA5')
- Quote No + Date
- Client name and address
- Subject line
- Items table using autoTable
- Totals (subtotal, sgst, cgst, grand total)
- Notes if any
- Footer: "Generated by Remindi · remindi.online"

Logo handling (skip if fails):
try {
  if (profile?.logo_url) {
    doc.addImage(profile.logo_url, 'PNG', 15, 10, 25, 25)
  }
} catch(e) { /* skip */ }

PDF filename: 
`Quotation-${quotation.quote_no ?? quotation.id}-${quotation.client_name ?? 'Client'}.pdf`

---

8. WHATSAPP SHARE FUNCTION

Build this message and open wa.me:

const msg = `*Quotation - ${safeStr(profile?.company_name)}*
Quote No: ${safeStr(quotation.quote_no)}
Date: ${safeDate(quotation.created_at)}
Valid till: ${safeDate(quotation.valid_till)}

*Customer:* ${safeStr(quotation.client_name)}

─────────────────────
*Services:*
${(quotation.items ?? []).map(i => 
  `• ${i.particulars ?? i.description ?? i.name} - Rs.${Number(i.amount ?? 0).toLocaleString('en-IN')}`
).join('\n')}
─────────────────────
*Subtotal:* Rs.${safeNum(quotation.subtotal)}
*GST (18%):* Rs.${safeNum((quotation.sgst ?? 0) + (quotation.cgst ?? 0))}
*Total:* Rs.${safeNum(grandTotal)}
─────────────────────

To confirm reply YES or call ${safeStr(profile?.phone)}

_Powered by Remindi_`

window.open('https://wa.me/?text=' + encodeURIComponent(msg))

---

9. ALSO FIX THE SAVE FUNCTION in /quotations/new

The grand_total is saving as 0. Fix the calculation before insert:

const subtotal = (items ?? []).reduce((sum, item) => {
  return sum + (Number(item.qty ?? 0) * Number(item.rate ?? 0))
}, 0)
const sgst = includeGst ? Math.round(subtotal * 0.09) : 0
const cgst = includeGst ? Math.round(subtotal * 0.09) : 0
const grand_total = subtotal + sgst + cgst

Include all 4 values in the Supabase insert:
{ subtotal, sgst, cgst, grand_total }

---

Rebuild only /quotations/:id view page and fix the 
/quotations/new save function.
Do not touch any other pages.